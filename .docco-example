[input]
# One or more markdown or HTML files to convert (relative to this file)
file = ["examples/Feature_Showcase.md"]
# or: file = "examples/Feature_Showcase.md"

[python]
# Allow execution of Python code in inline directives (security risk)
# allow = true

[output]
# Output directory for generated PDFs (relative to this file)
# path = "."
# createdir = true  # output to {path}/{filename without extension}/
# keep-intermediate = false  # keep intermediate .md and .html files

[multilingual]
# Output filename pattern for multilingual PDFs ({filename} and {langcode} are substituted)
# filename = "{filename}_{langcode}"
