# Production Dependencies
# Matches dependencies in pyproject.toml

# Markdown parsing
markdown-it-py==4.0.0
mdit-py-plugins==0.5.0

# Frontmatter parsing
python-frontmatter==1.1.0
pyyaml==6.0.3

# PDF generation (not available on Windows via pip)
weasyprint==67.0; sys_platform != 'win32'

# Translation workflow
translate-toolkit==3.17.3
polib==1.2.0

# Utilities
colorlog==6.10.1
pymupdf==1.26.6
